services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: clink
      POSTGRES_PASSWORD: clink-local-only
      POSTGRES_DB: clink_core
    ports: ["127.0.0.1:5433:5432"]
    volumes: ["core-postgres:/var/lib/postgresql/data"]
volumes:
  core-postgres:
